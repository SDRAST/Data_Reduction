"""Module vdr_support allows raw VSR data files to be read.  The function is::
  unpack_vdr_data(buf)
where buf is a block of one second of packed binary data, not including the
record header."""
from vdr_support import *

